{
  "name": "wat-fft-playground",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "setup": "cd public && ln -sf ../../dist wasm",
    "postinstall": "npm run setup"
  },
  "dependencies": {
    "fft-js": "^0.0.12",
    "fft.js": "^4.0.4",
    "fftw-js": "^0.1.4",
    "kissfft-js": "^0.1.8"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
